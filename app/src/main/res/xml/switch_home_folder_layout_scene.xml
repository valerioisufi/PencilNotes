<?xml version="1.0" encoding="utf-8"?>
<MotionScene 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">

    <Transition
        motion:constraintSetEnd="@+id/select_folder"
        motion:constraintSetStart="@id/select_home"
        motion:duration="250">
       <KeyFrameSet>
       </KeyFrameSet>
        <OnClick motion:targetId="@+id/disactive_background" />
    </Transition>

    <ConstraintSet android:id="@+id/select_home">
    </ConstraintSet>

    <ConstraintSet android:id="@+id/select_folder">
        <Constraint
            android:id="@+id/disactive_background"
            android:layout_width="0dp"
            android:layout_height="0dp"
            motion:layout_constraintBottom_toBottomOf="parent"
            motion:layout_constraintTop_toTopOf="parent"
            motion:layout_constraintStart_toStartOf="parent"
            motion:layout_constraintEnd_toStartOf="@id/switch_guideline" />
        <Constraint
            android:id="@+id/active_background"
            android:layout_width="0dp"
            android:layout_height="0dp"
            motion:layout_constraintBottom_toBottomOf="parent"
            motion:layout_constraintTop_toTopOf="parent"
            motion:layout_constraintEnd_toEndOf="parent"
            motion:layout_constraintStart_toEndOf="@id/switch_guideline" />
    </ConstraintSet>
    <Transition
        motion:constraintSetStart="@+id/select_folder"
        motion:constraintSetEnd="@+id/select_home"
        motion:duration="250">
        <OnClick motion:targetId="@+id/disactive_background" />
    </Transition>
</MotionScene>